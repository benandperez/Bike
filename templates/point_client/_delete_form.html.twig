<form method="post" action="{{ path('point_client_delete', {'id': point_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ point_client.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('bike_type_delete', {'id': bike_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bike_type.id) }}">
    <button class="btn">Delete</button>
</form>
